# Журнал изменений

Дневник улучшений и решений по проекту — чтобы не путать его состояние с чужими
отчётами, старыми черновиками или памятью из предыдущих сессий. Каждое заметное
изменение фиксируется здесь одной-двумя строками, с датой и (где уместно) хешем
коммита. Порядок — от старого к новому.

## 2026-09-21 — Отложены дополнения к базе

- Убраны все 50 добавленных вопросов из опубликованной базы. Механизм проверки
  сохранён: следующие дополнения будут добавляться только после личной верификации.

## Это не то же самое, что...

`~/Documents/Codex/2026-09-17/html/outputs/lingvostranovedenie-quiz.html` —
самый первый черновик (данные в `work/question_bank.json`, функция `saveAppState`,
один брейкпоинт на 680px). Он заброшен 2026-09-18, когда началась полная
пересборка. Если какой-то отчёт, скриншот или другая сессия описывает
`question_bank.json`, ID вида `history-060-217`/`misc-020-393` или `saveAppState` —
речь про этот старый файл, не про текущий репозиторий.

## 2026-09-18 — Полная пересборка проекта

- Спека и план реализации (`docs/superpowers/specs/`, `docs/superpowers/plans/`):
  единый самодостаточный HTML, 7 категорий из базы «310», проверка каждого ответа
  по интернету волнами, даже при наличии официального ключа.
- Детерминированный пайплайн источников → `data/raw/` → `data/bank.json`:
  парсеры для банка 423, базы «310», сводного сборника ВсОШ и PDF-бюллетеней
  (`scripts/import_sources.py`, `scripts/parsers/*`).
- Слияние в канонический банк с защитой от обрезанных формулировок и ложных
  склеек (`scripts/merge_bank.py`), рубрикация по 7 темам (`scripts/taxonomy.py`).
- Логика раунда: стабильные `optionId`, буквы A–D только по видимому порядку
  после перемешивания (`src/quiz.mjs`).
- Сборка одного файла с проверкой инвариантов на этапе сборки
  (`scripts/build_html.py` → `dist/lingvostranovedenie-trainer.html`).
- Волны проверки 1–5 (421 вопрос): у каждого ответа — пояснение и ссылка на
  источник; расхождения с официальным ключом не решаются молча, уходят в
  `conflict` и в отчёт волны.
- Дизайн восстановлен по образцу старого черновика (карточки, синий акцент),
  добавлены светлая/тёмная тема и полное управление с клавиатуры
  (`1459e86`).

## 2026-09-19 — Пост-релизные улучшения

- `cbc61be` — минификация встраиваемых CSS/JS/JSON при сборке (626 КБ → 597 КБ),
  без новых зависимостей.
- `37d592d` — волна 6: два оставшихся расхождения с официальным ключом разобраны
  и подтверждены по Википедии (флаг `overrideOfficialKey`, отличается от
  молчаливого конфликта). **С этого коммита банк — 421/421 verified, 0 needs-review,
  0 conflict.**
- `3b691ba` — экспорт/импорт прогресса в JSON-файл (кнопки «Сохранить/Загрузить
  прогресс»).
- `1ff83dc`, `e1fa414` — публикация на GitHub Pages через GitHub Actions: при
  каждом push банк пересобирается из первоисточников, гоняются оба набора
  тестов, публикация — только если всё зелёное.
  Демо: https://bronik04.github.io/lingvostranovedenie-trainer/
- `b598f3c` — favicon (инлайновый SVG) и Open Graph/Twitter превью
  (`site-assets/og-image.jpg`).
- `0f64b51` — бейдж статуса CI в README.
- `1a53f9d` — кнопка «Новый раунд» на экране результатов переименована в
  «К настройкам»: она и раньше вела на главную, но название вводило в
  заблуждение.
- `f3598e0` — три явных уровня адаптивности (ноутбук / планшет ≤1024px /
  телефон ≤600px); почищено реальное переполнение на ширине iPhone
  (320–430px): непереносимое слово в `h1` и `white-space: nowrap` у плашки
  счётчика.
- `e22b080` — главная страница: настройки раунда свёрнуты под `⚙ Настройки
  раунда` (`<details class="settings">`), подзаголовок под H1 заменён с
  инструкции по управлению на описание сути проекта.
- `bd87a4c` — заведён этот журнал (повод: внешний отчёт описывал заброшенный
  черновик как будто это текущий проект — см. «Это не то же самое, что...»
  выше).
- `d66f3e5` — `manifest.json` + `theme-color` для «Добавить на экран»: та же
  SVG-иконка, что у favicon, встроена как data URI (PNG 192/512 +
  apple-touch-icon 180), работает и офлайн. +22 КБ, без service worker.
- `8c819c9` — фильтр «Только с ошибками» (работа над ошибками): переиспользует
  уже отслеживаемый `progress[id].lastCorrect`, новых полей не потребовалось.
- `b459aa6` — интервальное повторение: упрощённый Leitner (не полноценный
  SM-2 — от клика по варианту есть только бинарный верно/неверно, более
  тонкая шкала не даст сигнала) поверх того же `progress[id]`. Переключатель
  «Пора повторить» сортирует раунд по просрочке вместо перемешивания.
- `f85f690` — еженедельная проверка ссылок-источников в CI
  (`.github/workflows/check-links.yml`, `scripts/check_sources.py`), никак
  не влияет на сам сайт. По пути на реальных ~327 ссылках банка нашлись и
  исправлены три настоящих бага чекера (см. коммит для деталей): кириллица/
  иероглифы в URL валили `http.client` без процент-кодирования;
  `baike.baidu.com` не поддерживает HEAD как положено; и главное — один
  прогон ничего не доказывает (антибот-блокировки и TLS-таймауты китайских
  gov.cn массово давали ложные 403/timeout под нагрузкой прогона). Теперь
  ссылка «подтверждается» битой только по НАСТОЯЩЕМУ HTTP-ответу (404 и
  т.п.), повторившемуся два прогона подряд — 403 и сетевые ошибки в
  принципе не подтверждаются. На реальных данных: 91 «битая» по наивной
  логике → 9 подтверждённых настоящих 404.

- Исправлен совместный фильтр года и этапа: оба условия относятся к одному
  появлению вопроса; подпись в раунде показывает выбранное появление.
- Импорт и загрузка прогресса проверяют записи до сохранения, поэтому повреждённый
  JSON не ломает тренажёр при следующем открытии.
- Данные банка безопасно встраиваются в HTML и экранируются при выводе;
  ссылки на источники допускают только HTTP(S).
- Девять подтверждённо нерабочих адресов источников заменены проверенными
  страницами; обновлены сведения для 11 вопросов.

## 2026-09-20 — Небольшие улучшения тренажёра

- «Пора повторить» выбирает только уже пройденные вопросы с наступившим сроком;
  на экране настроек видно, сколько их доступно.
- После раунда можно отдельно повторить вопросы, в которых были ошибки.
- В базе ответ и пояснение раскрываются по нажатию «Показать ответ».
- Рядом с каждой темой показывается число пройденных вопросов из общего числа;
  кнопка «Сбросить фильтры» очищает отбор, сохраняя размер раунда.
- Первый экран упрощён: один режим («Все вопросы», «Новые», «Ошибки» или
  «Повторение») заменил пересекающиеся флажки, а управление прогрессом
  свёрнуто отдельно от начала раунда.
- Пустой результат фильтров предлагает сбросить их или начать обычный раунд;
  в ответах явно подписываются верный и выбранный варианты.
- База загружает карточки порциями по 50 и сохраняет уже раскрытые ответы;
  счётчик показывает, сколько карточек уже открыто.
- На телефоне уменьшены верхние отступы; после ответа разбор получает фокус и
  плавно появляется в области просмотра, если не включено уменьшение движения.
- Поиск в базе теперь находит текст вопроса, тему, китайские варианты, правильный
  ответ и пояснение; подпись поля поиска перечисляет эти варианты.
- Выбранные темы, год, этап, режим, размер раунда и фильтр проверенности
  восстанавливаются после перезагрузки из отдельной локальной записи.
- Ошибочный вопрос возвращается один раз через три следующих задания, а при
  нехватке заданий — перед итогами раунда.
- Повторная проверка ошибок не увеличивает размер исходного раунда в итоговом
  счёте; кнопки возврата с экрана результатов и базы названы «На главную».

## 2026-09-21 — Дополнения к олимпиадной базе

- Новые задания хранятся отдельно, проходят проверку формата, подтверждения и
  уникальности, а в раунде и базе явно помечаются как «Дополнение к
  олимпиадной базе».
- В раскрытой карточке базы появились ссылки на источники; первая волна добавила
  25 подтверждённых обратных и контекстных вопросов с официальными ссылками.
- 25 коротких пояснений олимпиадных заданий переписаны в формате трёх полезных
  предложений: факт, контекст и причина выбора ответа.
- Дополнения участвуют в обычных раундах, но исключаются при отборе по году или
  этапу олимпиады; это покрыто отдельным тестом.
- Вопросы первой волны переработаны в обратные, а восемь ссылок в пояснениях
  заменены с главных страниц ведомств на подтверждающие материалы.
- Вторая волна добавила ещё 25 самостоятельных заданий по административному
  устройству, государству, обществу, праздникам и базовой финансовой лексике.

## 2026-09-23 — Новые дополнения: партия 1

- Порядок сбора: сначала автор проекта утверждает подтемы (утверждены все 40 в
  семи категориях), затем партиями по 20 — сами вопросы; в проект попадают только
  принятые. Требования: сложные страноведческие (не языковые) вопросы, минимум
  два независимых официальных или академических источника, проверка дистракторов.
- Партия 1: 20 вопросов из 20 разных подтем, приняты все (`addition-b1-01…20`,
  новые id, чтобы не подхватить прогресс по удалённым `addition-001…050`).
  Цитаты из источников — в `data/review/additions-batch-1.md`. Банк: 441 вопрос.
- Проверка дополнений (`scripts/authored.py`): допускаются источники `academic`,
  а не только `official`; требуется не меньше двух источников.
- Подсчёт предложений в пояснениях больше не дробит предложение на
  сокращениях («1858 г. в…», «т. е.») и инициалах («Н. Н. Муравьёв»).
- Партия 2: 20 вопросов по 20 подтемам, не затронутым в партии 1, приняты все
  (`addition-b2-01…20`). Один кандидат (самая населённая провинция по переписи
  2020 г.) заменён до показа как дубль олимпиадного 2018-19-shk-3. Цитаты и
  оговорки проверяющего — в `data/review/additions-batch-2.md`. Банк: 461 вопрос.

## 2026-09-24 — Вычитка формулировок: серьёзные и средние правки

- Вычитаны все 461 вопрос; найдено 157 замечаний (12 серьёзных, 53 средних,
  92 мелких). Автор проекта утвердил серьёзные и средние — все 65 применены;
  мелкие ждут разбора.
- Новый слой `data/review/editorial.json` + `scripts/editorial.py`: правки
  накладываются после слияния по итоговому id (с проверкой языка, вариантов,
  темы и обязательной причиной), поэтому id и прогресс учеников не меняются.
  Тема из правки помечается `topicSource: editorial` и не перезаписывается
  рубрикатором.
- Среди исправленного: «блокада Ленинграда советскими войсками», «правительство
  СССР» в 1918 г., устаревшие «действующий премьер/председатель», два верных
  варианта (урожаи, Чжоукоудянь, климат), опечатки в вариантах (黄帝→皇帝,
  郭莫柔, 神州, 契科夫, 最高人们法院, 后裔射阳, повтор 大唐芙蓉园), калька
  «история цигун в годах», ошибки в пояснениях (площадь Китая и Внутренней
  Монголии, Тайвань как колония, Потала).

## 2026-09-24 — Исправления по итогам осмотра

- Описание сайта для поисковиков и превью ссылок больше не устаревает: число
  вопросов подставляется при сборке (`bank_description` в `scripts/build_html.py`),
  было захардкожено «421 вопрос» при 461 в банке.
- Подвал считает всё по банку (`bankSummary` в `src/quiz.mjs`): вопросы, дополнения
  и годы, с правильным склонением («421 вопрос», а не «421 вопросов»). Фраза про
  ответы, которые «ждут перепроверки», появляется, только если такие ответы есть.
- Картинка превью `site-assets/og-image.jpg` перерисована без чисел («421 вопрос ·
  проверено 421» устарело); у неё появился исходник `site-assets/og-image.html` и
  скрипт `scripts/render_og_image.py` (нужен Chrome, в сборку не входит).
- Проверка ссылок-источников (`scripts/check_sources.py`) чинена по двум реальным
  сбоям. Прогон 2026-09-21 упал целиком на обрыве соединения
  (`http.client.RemoteDisconnected` не ловился), а CI принял падение за «ссылки
  битые» и открыл issue со старым отчётом; теперь обрывы, сбросы и TLS — сетевой
  шум, подтверждённые битые ссылки выходят с кодом 3, а падение скрипта валит
  прогон. Второе: решает только ответ на GET — три живые страницы Baike числились
  «404» из-за HEAD (Baike отвечает 404 на любой HEAD), когда GET упирался в
  антибот. Коды 403/418/429 считаются защитой от ботов, а не ответом про страницу.
- Заменены два источника: удалённая страница beijing.gov.cn (`2015-16-mun-4`) —
  на страницу Управления парков Пекина, и csx.gov.cn (`2017-18-shk-5`, WAF не
  пускает даже обычный браузер) — на раздел «国情» сайта gov.cn.
- Применены все 92 мелкие правки вычитки (по решению автора — все как
  предложено); с ними вычитка закрыта: 157 из 157. Среди них: опечатки в
  иероглифах (铁观念茶, 有作位 в китайском тексте вопроса, 最高宪法法院, 太梅尔半岛,
  罗巴契夫斯基), служебные пометки «официальный ключ» / «решение владельца проекта»
  в пояснениях, привязка «текущих» фактов к дате (купюры образца 1997 года,
  вокзалы Москвы на 2024 год), подсказки ответа в двух дополнениях, устаревшая
  численность чжуанов (перепись 2020 года). Одна правка шире замечания:
  «почти 88% живут в Гуанси» — доля по переписи 2000 года, заменена на
  «большинство» по официальному источнику вопроса.
- Слой правок `editorial.json` умеет править китайский текст вопроса
  (`questionZh`) и накладывается после дополнений, а не до: девять правок
  касались дополнений, прежний порядок их бы отверг. Тест проверяет, что каждая
  правка из файла дошла до `data/bank.json`.

## Не реализовано (сознательно или пока)

- Печатные стили (`@media print`) — не запрашивались, не делались.
